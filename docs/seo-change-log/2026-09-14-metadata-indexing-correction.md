# Metadata/indexability accuracy correction — 2026-09-14

Evidence/source base: `266dc3c84848bdb8c2d9510bc65fa0ce93db173c`. Approved by Matt after the post-publication audit. **Source release only; Lovable publication pending.** No publication timestamp is inferred from this document or Git commit.

## Exact scope

- `/intervention-toolkit`, `/how-intervention-works`, `/service-areas`: the fallback generator treated these unmapped hyphenated slugs as locations. Initial title, description and noscript H1 contradicted the existing page components. Add an explicit reviewed source-file mapping which reads their existing SEOHead title/description, H1 and opening paragraph. Fail if those literal contracts change instead of silently returning a geographic fallback. Keep the existing shared title/description formatting; no new snippet experiment or body copy.
- The controlled Chromium reproduction on this source base found correct **settled local** page metadata, unlike the audit's settled live fallback. Do not claim a proven Helmet library defect or byte-identical live bundle. Remove the competing DefaultSEO instance on these three owned routes; retain the existing Helmet-managed-tag reconciliation. Both initial output and settled/direct/SPA metadata must now agree without depending on browser execution to correct a location label.
- `/next-step`: explicitly `noindex, follow` in initial and runtime head; exclude from sitemap generation and the versioned sitemap. Keep the public, no-signup guide, choices, optional contact handoff and normal access unchanged. Retain crawler access to read noindex (no robots.txt disallow); add it to the worker's routable allowlist, not the indexable sitemap. Strip reintroduced Google Fonts hints/stylesheet links from **this route's generated document only** so the private guide stays same-origin during normal use. Public typography, admin/family-portal guards, attribution and backend behavior are unchanged.
- Legacy `http://www.freedominterventions.com/post-sitemap.xml` was observed redirecting to HTTPS then 404 in the audit. Add a static XML sitemap index at `/post-sitemap.xml` referring to the existing canonical `/sitemap.xml`. No DNS, host redirects or GSC submissions/deletions changed. It does not claim complete coverage of historical WordPress URLs: no authoritative old sitemap archive was available. Verify the exact legacy redirect chain and XML after publication before considering GSC cleanup. The repository worker passes XML through; its route allowlist amendment is source-only, not a claimed independent Worker deployment.

## Preservation and observation

No regional titles/body copy, seven-state navigation, winner articles, cost answer, medical guidance, phone numbers, billing/auth, migration/Edge Functions, central attribution, GA4 payloads or other brands changed. No operational ledger/cron changes or cooldown resets. This is an accuracy/privacy-intent exception, not a ranking rewrite. Preserve existing publication-relative 28-day metadata and 60-day body cooldowns. After Matt publishes, read back the exact changed routes and sitemap; record only the actual observation/publication evidence then. No rankings, traffic gains or whole-site publication claims.

## Verification

`node scripts/test-metadata-indexing.mjs` after the full production build verifies an isolated real initial generator run (both hosting variants), raw HTTP, settled Chromium at 390/1280, SPA navigation, exact head ownership, schema parsing, H1, same-origin guide requests, no form opening/submission, overflow and parseable sitemap/index coverage. Set `PLAYWRIGHT_CHROME_PATH` when needed. `SEO_TEST_BASELINE=<SHA>` reproduces the old geographic-title failure without editing the worktree. `SEO_TEST_ARTIFACTS=<directory>` writes evidence outside source.

The current upstream npm lockfile is out of sync with package.json. `npm ci` fails before this change; validation used `npm install --no-package-lock` without changing dependencies/lockfiles. Full Chromium prerender and all relevant tests are recorded in `/root/fi-metadata-indexing-fix/`; browser QA blocks third-party events and forms, and the prerender-only guard permits read-only published blog fetches. GitHub sync and Lovable publication remain separate gates.
