# Copy to .env and fill in real values. NEVER commit the real .env.
# Public (safe to expose in the browser bundle) — protected by Supabase RLS.
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# NOTE: Do NOT add secret API keys with a VITE_ prefix — anything VITE_* is
# inlined into the public client bundle. Secrets belong in Supabase/Lovable
# Edge Function environment variables, accessed via Deno.env.get() server-side.
